

export default function InfoCard({
  label,
  title,
  body,
}: {
  label: string;
  title: string;
  body: string;
}) {
  return (
    <article className="info-card">
      <p className="info-label">{label}</p>
      <h2>{title}</h2>
      <p>{body}</p>
    </article>
  );
}
